# AGENTS.md

## Agent skills

### Issue tracker

Issue 与 spec 以 markdown 文件形式存放在仓库内 `.scratch/` 下。详见 `docs/agents/issue-tracker.md`。

### Triage labels

直接使用五个标准标签:`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix`。详见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文布局:根目录 `CONTEXT.md` + `docs/adr/`。详见 `docs/agents/domain.md`。
