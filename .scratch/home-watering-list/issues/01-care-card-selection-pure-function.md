# 01 — 养护卡片选取提取为纯函数(与列表排序解耦)

**What to build:** 顶部养护卡片的选取不再依赖列表排序的结果,而是从全部植物中独立选取"最近养护"最新的一株植物,养护卡片继续展示该植物的最近养护。由于当前列表排序恰好按"最近养护",本票完成后养护卡片的显示结果与现状完全一致——这是为后续切换列表排序做解耦准备,不引入任何用户可见的行为变化。

**Blocked by:** None — can start immediately

**Status:** resolved

- [x] 养护卡片选取逻辑由植物领域逻辑层的纯函数提供,页面层只消费选取结果
- [x] 选取对象为全部植物中"最近养护"时间戳最大的一株
- [x] 所有植物均无养护记录时,保持现有兜底行为(回退到列表第一株植物与默认文案)
- [x] 养护卡片文案模板与消费字段不变,改动前后显示结果一致
- [x] 新增选取函数的单测,覆盖:正常选取(允许选取结果与列表首位不同)、全部植物无养护记录时的兜底
- [x] 现有测试全部通过

## Answer

新增纯函数 `selectMissionPlant`(时间戳最大者优先,同值按名称升序),页面层 `buildMissionCard` 改为消费其结果;行为与改动前完全一致。测试见 `test/plants-mission-card.test.js`。双轴评审通过(1 条判断题级气味:与 `sortPlantsByTask` 的排序形状重复,待票 02 后复评)。
