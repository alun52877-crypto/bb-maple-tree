# 03 — 移除首页模块死代码与未引用的列表状态字段

**What to build:** 删除首页模块中不可触达的代码与无引用的字段,使代码库不再误导后续修改。包括:首页中没有入口触发的快捷浇水记录逻辑及其伴随的请求去重状态、因此不再使用的存储函数导入,以及未被任何模板引用的列表状态装饰字段(含其在写库剥离清单中的对应条目)。

**Blocked by:** None — can start immediately(建议排在 01/02 之后执行,避免同文件改动摩擦)

**Status:** resolved

- [x] 不可触达的快捷浇水记录逻辑及其请求去重状态被删除,相关导入收窄,无残留引用
- [x] 未被引用的列表状态装饰字段被删除,写库剥离清单中的对应条目同步删除
- [x] 现有功能(列表展示、排序、养护卡片、详情页)行为与改动前一致
- [x] 现有测试全部通过

## Answer

删除首页不可触达的 `markWatered`/`updateCareRecord` 及 `actionKey` 状态、收窄存储导入至 `getPlantsPage`;删除 `listStatus` 装饰字段及写库剥离清单对应条目。全量测试通过,无残留引用。注:`pages/plant-detail/` 中同名的 `markWatered` 是详情页真实入口,不属于本票删除范围。
